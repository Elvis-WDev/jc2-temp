# GitHub Copilot Instructions

Read `AGENTS.md` first.

Use `docs/` as the source of truth for product, architecture, quality, and security context.

For path-specific guidance, use files in `.github/instructions/`.


# Codex Configuration Directory

Use `.codex/` only for local Codex configuration when needed.

Do not store product requirements, architecture decisions, feature specs, or plans here. Codex does not automatically treat arbitrary Markdown in `.codex/` as project instructions.

Project knowledge belongs in:

- `AGENTS.md`
- `ARCHITECTURE.md`
- `docs/`
- `.agents/skills/`

